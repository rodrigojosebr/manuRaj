# =============================================================================
# manuRaj - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in the values

# =============================================================================
# MongoDB Connection (MongoDB Atlas - Recomendado)
# =============================================================================
# Passos para configurar MongoDB Atlas:
# 1. Crie uma conta gratuita em: https://www.mongodb.com/atlas
# 2. Crie um cluster (M0 Free Tier é suficiente para dev)
# 3. Em "Database Access", crie um usuário com senha
# 4. Em "Network Access", adicione seu IP (ou 0.0.0.0/0 para permitir de qualquer lugar)
# 5. Clique em "Connect" > "Connect your application" e copie a connection string
# 6. Substitua <password> pela senha do usuário criado
#
# Formato da connection string:
MONGODB_URI=mongodb+srv://usuario:senha@cluster.mongodb.net/manuraj?retryWrites=true&w=majority

# Para Docker local (opcional - requer Docker instalado):
# MONGODB_URI=mongodb://admin:admin123@localhost:27017/manuraj?authSource=admin

# =============================================================================
# NextAuth.js Configuration
# =============================================================================
# Generate a secret: openssl rand -base64 32
AUTH_SECRET=your-super-secret-key-change-in-production

# The base URL of your application
NEXTAUTH_URL=http://localhost:3000

# Trust host header (required for Vercel)
AUTH_TRUST_HOST=true

# =============================================================================
# AWS S3 Configuration
# =============================================================================
# AWS credentials for S3 document uploads
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# S3 bucket name for document storage
S3_BUCKET_NAME=manuraj-documents

# =============================================================================
# Application URLs
# =============================================================================
NEXT_PUBLIC_APP_URL=http://localhost:3000

# =============================================================================
# Feature Flags (optional)
# =============================================================================
# Enable/disable signup (default: true)
ENABLE_SIGNUP=true

# Enable/disable ads globally (default: true)
ENABLE_ADS=true

# =============================================================================
# Google AdSense Configuration
# =============================================================================
# Publisher ID from Google AdSense (format: ca-pub-XXXXXXXXXXXXXXXX)
# Leave empty in development to show placeholder ads
NEXT_PUBLIC_ADSENSE_PUBLISHER_ID=

# =============================================================================
# Development Only
# =============================================================================
# Set to 'development' for local, 'production' for deploy
NODE_ENV=development

# =============================================================================
# Vercel Cron Jobs (for preventive plans scheduling)
# =============================================================================
# Secret token for cron job authentication
CRON_SECRET=your-cron-secret-token
